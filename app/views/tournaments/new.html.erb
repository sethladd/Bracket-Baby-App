<% @title = 'Create a Tournament' %>
<h1>New Tournament</h1>

<%= form_for @tournament do |f| %>
	<div>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>
	<div>
		<%= f.label :description %>
		<%= f.text_area :description %>
	</div>
	<div>
		<%= f.submit 'Create' %>
	</div>
<% end %>